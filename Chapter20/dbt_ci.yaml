models:
  - name: fact_orders
    columns:
      - name: order_id
        tests: [unique, not_null]
      - name: order_channel
        tests:
          - accepted_values:
              values: ['web','store','partner']
